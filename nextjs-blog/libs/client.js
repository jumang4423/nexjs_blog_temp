import { createClient } from 'microcms-js-sdk';

// microCMSからデータとってきてくれる関数
export const client = createClient({
  serviceDomain: 'babi-bloggggg',
  apiKey: "c5f0a8fd-94c2-4964-8d0c-22471c0beea1",
});
